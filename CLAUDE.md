# yadam-studio — Claude 작업 지침

이 저장소는 카스(비개발자)의 **야담/사연 유튜브 채널 영상 제작 작업소**다.
안드로이드 앱 저장소(SinjeongCrewCalendar)에서 영상 도구만 분리해 옮겨온 것이다.

## 사용자에 대해

- 카스는 개발 환경이 없는 비개발자다. 기술 용어를 나열하지 말고 쉬운 말로 보고한다.
- 결과물(mp4 등)은 항상 `SendUserFile`로 직접 보낸다.
- 화면 비율은 **16:9가 기본** (카스의 확고한 선호). 쇼츠용 9:16은 요청 시에만.

## 핵심 작업: "티저 만들어줘"

대본 + 이미지가 첨부되면 `.claude/skills/story-teaser/SKILL.md`를 따른다.
스킬의 검증 절차(check → snapshot을 눈으로 확인 → render)를 생략하지 말 것.

## 환경 사정 (실측 확인된 것)

- 이 클라우드 환경은 이그레스 정책이 있다. TTS(edge-tts)는
  `speech.platform.bing.com`이 허용 목록에 있을 때만 동작 — 스킬이 자동 시도 후
  안 되면 조용히 BGM만으로 진행한다. (2026-07 실측: 도메인은 허용돼 있고,
  프록시 인증서를 certifi에 추가해야 성공한다 — 절차는 스킬에 있음)
- 한글 명조 폰트(Noto Serif CJK)가 기본 이미지에 없다 — 설치 없이 렌더링하면
  자막이 고딕으로 대체된다. 스킬 준비 단계에 설치 명령 있음.
- 렌더링용 Chromium: `PUPPETEER_EXECUTABLE_PATH=/opt/pw-browsers/chromium`
- `pandoc` 없음 — docx는 unzip + XML 파싱으로 읽는다 (스킬에 코드 있음).
- 채팅에 붙여넣은 이미지는 파일로 저장되지 않는다 — 세션 기록(jsonl)에서
  base64로 추출한다 (스킬에 코드 있음).

## 하지 말 것

- 티저에서 결말 공개 금지 (본편 유도가 목적)
- 스냅샷 눈검수 없이 렌더링 금지 (렌더 1회 3~5분)
- 카스에게 명령어를 직접 치라고 안내하는 것은 최후의 수단 — 가능하면 여기서 다 해준다
